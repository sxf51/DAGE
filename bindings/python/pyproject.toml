[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dage-runtime"
version = "0.2.0"
description = "Python binding for the embeddable DAGE DAG runtime"
requires-python = ">=3.12"
license = { text = "MIT" }

[tool.setuptools]
py-modules = ["dage"]
