# ADR-0002：调度器与默认线程池

- 状态：Accepted
- 日期：2026-07-27

## 决策

Core 默认拥有有界线程池，同时允许宿主注入 Scheduler SPI。线程池不得为每个节点创建线程，
不得无界排队。嵌套 DAG 从同一池提交任务时允许 inline 执行，以避免线程饥饿死锁。

## 后果

默认配置开箱即用；已有事件循环、游戏引擎或专用调度系统可以接管执行。队列满、关闭和调度器
异常必须转换为结构化运行错误，不能导致未定义行为。
